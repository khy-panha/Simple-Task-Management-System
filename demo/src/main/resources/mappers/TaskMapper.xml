<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.task.mapper.TaskMapper">

  <resultMap id="TaskResultMap" type="com.example.task.model.Task">
    <id property="id" column="id" />
    <result property="userId" column="user_id" />

    <result property="title" column="title" />
    <result property="description" column="description" />
    <result property="status" column="status" />
    <result property="createdAt" column="created_at" />
    <result property="updatedAt" column="updated_at" />
  </resultMap>

  <select id="findAll" resultMap="TaskResultMap">
    SELECT * FROM tasks
  </select>

  <select id="findById" parameterType="long" resultMap="TaskResultMap">
    SELECT * FROM tasks WHERE id = #{id}
  </select>

  <insert id="insert" parameterType="com.example.task.model.Task" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO tasks (title, description, status, created_at, updated_at, user_id)
    VALUES (#{title}, #{description}, #{status}, #{createdAt}, #{updatedAt}, #{userId})

  </insert>

  <update id="update" parameterType="com.example.task.model.Task">
    UPDATE tasks
    SET title = #{title},
        description = #{description},
        status = #{status},
        updated_at = #{updatedAt},
        user_id = #{userId}
    WHERE id = #{id}
  </update>

  <delete id="delete" parameterType="long">
    DELETE FROM tasks WHERE id = #{id}
  </delete>

</mapper>
